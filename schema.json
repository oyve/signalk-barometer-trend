[{
	"type": "object",
	"properties": {
		"rate": {
			"title": "Pressure Sample Rate (s)",
			"description": "Example: 60, 600, 1200 (1, 10, 20 minutes). Min: 60 (default), Max = 1200",
			"type": "number",
			"default": 60
		},
		"altitude": {
			"title": "Altitude offset (m)",
			"description": "Sensor altitude offset to GPS altitude or default zero",
			"type": "number",
			"default": 0
		}
	}
}]